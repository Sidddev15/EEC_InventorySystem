# EEC Inventory System

Production-grade industrial inventory system for configurable filter manufacturing.

Core rule: `ProductVariant` is the operational entity for stock, transactions, and production.
